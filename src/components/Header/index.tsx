import React from 'react';

import { Logo, Nav } from './styles';

const Header: React.FC = () => {

	return (
		<header>
			Header
			<Nav>
				<Logo />
			</Nav>
		</header>
	)
};

export default Header;
